This repo contains Terraform manifest files that will build a Cost Management solution for Azure Subscriptions.

The solution is based on Azure Budgets, that was configured to have four tresholds which can be addapted to any needs.

When each treshold is reached an email will be sent to the owner of the subscription.
